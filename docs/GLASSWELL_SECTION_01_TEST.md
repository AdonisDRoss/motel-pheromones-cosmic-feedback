# GLASSWELL SECTION 01 — C2 ↔ C3 ↔ D3

## Included pages

```text
GLASSWELL_C2 — Civic Admin Street
GLASSWELL_C3 — Main Civic Gate
GLASSWELL_D3 — Red Concord Checkpoint
```

## Canvas sizes

```text
C2: 1448 x 1086
C3: 1536 x 1152
D3: 1448 x 1086
```

Each page uses its own native canvas. Phaser places all layers at origin `0,0`, scale `1`.

## Test route

```text
C2 south exit → C3 north entry
C3 north exit → C2 south entry
C3 east exit → D3 west entry
D3 west exit → C3 east entry
D3 east exit → E3 placeholder message
C2 north exit → C1 placeholder message
```

## Controls

```text
WASD / arrows = move
E / A = interact with current route trigger
F / X = debug depth/transition zones
C / Y = collision mask preview
ESC = title
```

## Art status

C3 is the style-locked page.

C2 and D3 are section-test pages. Their layers are Phaser-aligned and playable, but art should still be reviewed before final lock.

## Rule

Do not build B3 into this section until B3 base art is approved.
